@model IEnumerable<CarmenSandiego.Models.Experience>

@{
    ViewData["Title"] = "Experience - Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>@Html.ActionLink("Add New Experience", "Create")</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

    @foreach (var listExperience in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(model => listExperience.Name)
            </td>
            <td>
                @Html.DisplayFor(model => listExperience.Description)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id = listItem.ItemId }) |
                    @Html.ActionLink("Details", "Details", new { id = listItem.ItemId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = listItem.ItemId })
                </td>*@
        </tr>
    }
</table>